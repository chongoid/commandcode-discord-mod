import type {Conversation, Destination} from '../domain.js';
export interface IncomingRoute {channelId: string; guildId?: string | null; userId: string; isThread: boolean; parentChannelId?: string | null; starterMessageId?: string; mentionsBot: boolean}
export function routeIncoming(input: IncomingRoute, conversations: Record<string, Conversation>, configuredChannelId?: string): {conversationId?: string; destination?: Destination; createThread: boolean} {if (!input.guildId) return {conversationId: `dm:${input.channelId}`, destination: {kind: 'dm', channelId: input.channelId, userId: input.userId}, createThread: false}; if (input.isThread) {const conversation = Object.values(conversations).find(item => item.destination.kind === 'guild' && item.destination.threadId === input.channelId); return conversation ? {conversationId: conversation.id, destination: conversation.destination, createThread: false} : {createThread: false};} if (input.channelId === configuredChannelId && input.mentionsBot) return {conversationId: `starter:${input.starterMessageId || ''}`, createThread: true}; return {createThread: false};}
export function stripOwnMention(content: string, botId: string): string {return content.replace(new RegExp(`<@!?${botId}>`, 'g'), '').trim();}
